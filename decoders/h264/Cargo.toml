[package]
name = "h264"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes.workspace = true
common = { path = "../../common" }
derive-try-from-primitive.workspace = true
etna = { path = "../../etna" }
